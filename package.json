{
  "name": "psixologiya-testbase",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:build": "npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
